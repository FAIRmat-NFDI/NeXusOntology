name: Build GitHub Pages

on:
  push:
    branches:
      - main
      - r2025_11
      - gh-pages-docs
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/configure-pages@v6
      - name: Download Widoco
        env:
          widoco_url: https://github.com/dgarijo/Widoco/releases/download/v1.4.25/widoco-1.4.25-jar-with-dependencies_JDK-11.jar
        run: wget ${widoco_url} -O widoco.jar
      - name: Convert RDF
        env:
          ont_file: ontology/NeXusOntology_full.owl
        run: |
          java -jar widoco.jar -ontFile ${ont_file} -outFolder ./_site \
               -confFile .github/widoco/widoco.properties \
               -getOntologyMetadata \
               -includeAnnotationProperties
          mv ./_site/index-en.html ./_site/index.html
      - uses: actions/upload-pages-artifact@v5

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: GitHub Pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
